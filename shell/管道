从结构化文件中提取数据：
umask 077：限制临时性文件只有我们可以访问 .
做一个简单的办公室名录文本文件：
join使用一个五步骤的管道完成：
  1.结合个人信息和办公室信息：join -t: $PERSON $OFFICE | ...这个运算的结果将成为下一步骤的输入
  2.加入电话号码： ... | join -t: -$TELEPHONE | ...
  3.删除键值（也就是第一个字段），因为我们不再需要它，... | cut -d: -f 2- | ...
  4.数据重新排序。... | sort -t: -k1,1 -k2,2 -k3,3 | ...
  5.重新格式化输出，使用awk和print语句，配合制表符（Tab）分割每个字段，
    ... | awk -F: '{ print("%-39\t%s\t%s\t%s\n",$1,$2,$3)}'
针对web的结构型数据：
...
文字解密：
...
单词列表：
...
标签列表：
...
